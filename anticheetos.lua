-- by carrot
-- unfinished Lol XD
-- this is a script with parent of `game` with name "Cheeto Dust" it is not disabled ok thanks
local Players = game:GetService("Players")

--[[ unfinished h
local function createThread(player)
	Spawn(function()
		while wait(1) do
--]]
Players.PlayerAdded:connect(function(player)
	--local ac = script.AC:Clone()
	--ac.Parent = player.PlayerGui
	--ac.Disabled = false
	--createThread(player)
end